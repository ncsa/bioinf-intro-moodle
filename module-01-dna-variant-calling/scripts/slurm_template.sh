#!/bin/bash
#SBATCH --time=		# Wall time of the job 
#SBATCH --job-name=	# user-defined job-name 
#SBATCH --account= 	# the account to charge 
#SBATCH --partition=	# which queue are we using 
#SBATCH --output=	# stdout of the linux commands, usually ${job-name}.o%j 
#SBATCH --error=	# error message, usually ${job-name.e%j

# load modules with 'module load ${MODULE_NAME}'

# set up variables with 'export VARIABLE=${VARIABLE_VALUE}'

# linux commands that calls the functions 

 
